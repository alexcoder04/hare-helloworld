// use fmt library
use fmt;

// main function
export fn main() void = {
	// the ! tells the compiler to ignore any errors
	fmt::println("Hello world!")!;
}; // don't forget the ; after every function
